/* import {searchAsyncAction} from "../../actions/week/weekActionCreator";

export const mapStateToProps=(state)=>({

})

export const mapDispatchToProps=(dispatch)=>({
    handleSearchAsyncData(){
        dispatch(searchAsyncAction())
    }
}) */